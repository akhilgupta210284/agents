# RAGAS evaluation dependencies
# Install with: pip install -r requirements-eval.txt
# Requires Python >= 3.10

ragas>=0.2.0
datasets>=2.18.0
# LangChain Bedrock adapter (already in base requirements — listed here for explicitness)
langchain-aws>=0.1.0
